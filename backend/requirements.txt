# ── Core API ──────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── LLM / Orchestration ───────────────────────────────────────────────────────
google-genai>=1.0.0        # Person A: Gemini agents (domain classifier, PM, UI, backend, marketing, investor, skeptic)
anthropic>=0.26.1          # Person B: wireframe_gen HTML generation via Claude
langgraph>=0.1.0           # LangGraph StateGraph for multi-agent pipeline orchestration

# ── Artifact Generation ───────────────────────────────────────────────────────
python-pptx==0.6.23        # pitch deck slides
Pillow>=10.4.0             # image handling for pptx thumbnails (10.3.0 incompatible with Python 3.14)

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1       # .env loading
aiofiles==23.2.1           # async file I/O
httpx==0.27.0              # async HTTP client (health checks)
